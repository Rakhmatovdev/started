
A220523F8213


Rahmatov Jasur F82

1
#include <iostream>
using namespace std;

int main() {
int a;//sutka
cout<<"a= ";cin>>a;
int b;//soat
cout<<"b= ";cin>>b;
cout<<a*60*24+b*60;
    return 0;
}

2

#include <iostream>
using namespace std;
int main() {
    int a, b, c;
    cout << "a: ";
    cin >> a;
    cout << "b: ";
    cin >> b;
    cout << "c: ";
    cin >> c;
    if (a > 0 && b > 0 && c <= 0) { cout << "a+b= " << a + b; }
    else if (a > 0 && c > 0 && b <= 0) { cout << "a+c= " << a + c; }
    else if (c > 0 && b > 0 && a <= 0) { cout << "c+b= " << c + b; }
    else { cout << "a*b*c= " << a * b * c; }

}


3

#include <iostream>
using namespace std;
int main() {
    for (int a = 100; a < 1000; ++a) {
        int r1 = a % 10;
        int r10 = a / 10 % 10;
        int r100 = a / 100 % 10;
        int r = r1 + r10 + r100;
        if (r == 20) {
            cout << a <<" ";
        }
    }
    return 0;
}

4

#include <iostream>
using namespace std;

int main() {
int a,ekub;
cout<<"a= ";cin>>a;
int b;
    cout<<"b= ";cin>>b;
    for (int i = 1; i <= min(a,b); ++i) {
        if(a%i==0 && b%i==0){
            ekub=i;
    }
    }cout<<ekub;
    return 0;
}


5

#include <iostream>
using namespace std;
int main() {
    string str;
    cout << "Input: ";
    cin >> str;

    int count = 0;


    for (int i = 0; i < str.length(); ++i) {


    } cout<<"Output: "<<str[0]<<str[str.length()-1]<<endl;
main();
}


6

#include <iostream>
#include <ctype.h>
using namespace std;
int main() {
    string str;
    cout << "Input: ";
    cin >> str;
    for (int i = 0; i < str.length(); ++i) {
if(isdigit(str[i])){ str[i]=0;continue;}

    } cout<<"Output: "<<str<<endl;
main();
}


